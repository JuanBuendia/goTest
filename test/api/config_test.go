package api_test

/*
func Config() {
	config, err := utils.LoadConfig(".")
	if err != nil {
		log.Fatal("cannot load config:", err)
	}

	models.ConnectDataBase(config.DBDriver, config.DBSource)

	server, err := api.NewServer(config)
	if err != nil {
		log.Fatal("cannot create server:", err)
	}
	fmt.Println(server)

	server.Start()

}
*/
